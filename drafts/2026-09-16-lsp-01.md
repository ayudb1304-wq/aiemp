# Review: New claim line-items table and claim-table columns vs. sign-off position

_Draft for `2026-09-16-lsp-01` (Ayush: Review the new lsp_claim_line_items table and added claim-table columns against the no-further-master-data-changes position agreed with Adwaith.). Written 2026-09-16 from CR_DISTRIBUTOR_LINE_ITEM_VISIBILITY_Revised (1).docx. Not sent anywhere; copy what you need._

**To:** Adwaith
**From:** Ayush
**Re:** CR - Distributor Line-Item Visibility and Claim Status Sync — conflicts with no-further-changes position

Adwaith,

I reviewed the CR against the position we agreed on 2026-09-16 (no further master data or DB column changes after sign-off). This CR introduces changes that conflict with that position:

1. **New table `lsp_claim_line_items`** (section 8.1) — 35 new columns to support line-item level claim data, with a unique constraint on (lsp_claimid, line_id), an FK to the claim table, and a secondary index on (lsp_claimid, MT_Chain_Name).
2. **New columns on the existing claim table** — `lsp_fy`, `lsp_totalnrclaimwithsr`, `lsp_totalpoclaimwithsr`, plus the status-sync fields: `lsp_statuscode`, `lsp_statustext`, `lsp_nrdocumentnumber`, `lsp_podocumentnumber`, `lsp_postingdate`, `lsp_errormessage`, `lsp_lastupdated`.

The chain table itself has no changes, but the above two items are new master data/schema changes landing after the sign-off cutoff.

Given the risk we flagged (each accepted change reopens the sign-off), I need your call on how to proceed: either these go through as an explicit, scoped exception to the sign-off, or we hold this CR until a proper re-sign-off cycle. Let me know which way you want to go before this gets built.

Ayush
