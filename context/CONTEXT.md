# Context has moved

The agent's memory is now a folder. This file is kept so old links work.
- `context/company.md`: me, roster, name map, priority rules, escalation, standing decisions.
- `context/projects/<slug>.md`: one file per project (goal, phase, next milestone, risks, people, glossary, recent decisions).
- `context/people/<name>.md`: optional per-person notes, created on demand.
